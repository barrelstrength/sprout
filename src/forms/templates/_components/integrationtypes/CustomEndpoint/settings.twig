{% import "_includes/forms" as forms %}
{% import "sprout-module-core/_macros/sproutcp" as sproutCp %}

{{ forms.textField({
    label: 'Submit URL'|t('sprout-module-forms'),
    instructions: 'Send your form data to this URL after a form is submitted.'|t('sprout-module-forms'),
    name: 'submitAction',
    placeholder: 'https://example.com/somewhere-else'|t('sprout-module-forms'),
    value: integration.submitAction ?? null,
    errors: integration.getErrors('submitAction') ?? null
}) }}
