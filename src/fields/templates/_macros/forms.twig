{% import "_includes/forms" as forms %}

{# Form Input Macros #}

{% macro phone(config) %}
    {% include "sprout-module-fields/_components/fields/Phone/input" with config %}
{% endmacro %}

{# Form Field Macros #}

{% macro phoneField(config) %}
    {% import "_includes/forms" as forms %}
    {% import _self as sproutFields %}
    {{ forms.field(config, sproutFields.phone(config)) }}
{% endmacro %}
