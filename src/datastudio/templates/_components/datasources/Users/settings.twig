{% import "_includes/forms" as forms %}

{{ forms.checkboxSelectField({
    label: "User Groups"|t('sprout-module-data-studio'),
    instructions: "Select the user groups to include in your report. All by default."|t('sprout-module-data-studio'),
    name: "settings[userGroupIds]",
    options: userGroupSettings,
    errors: errors.userGroupIds is defined ? errors.userGroupIds : '',
    values: settings.userGroupIds is defined ? settings.userGroupIds : null,
    first: true,
}) }}

{{ forms.lightswitchField({
    label: "Display User Groups",
    instructions: "Display user groups as columns in your results",
    name: 'settings[displayUserGroupColumns]',
    on: settings.displayUserGroupColumns is defined ? settings.displayUserGroupColumns : null,
}) }}
