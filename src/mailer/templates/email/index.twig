{% extends "sprout-module-mailer/_layouts/elementindex" %}
{% import "sprout-module-core/_macros/sproutcp" as sproutCp %}

{% set module = sprout.modules.mailer %}

{% block actionButton %}
    <div class="buttons">
        <a href="{{ newButtonUrl }}" class="btn submit add icon">
            {{ newButtonLabel }}
        </a>
    </div>
{% endblock %}

{# {% block initJs %} #}

{# Craft.defaultIndexCriteria = { emailType: '{{ className(emailType)|e("js") }}' }; #}

{# {{ parent() }} #}

{# {% endblock %} #}
